name: build_ErfanGSIs

on:
#  release:
#    types: [published]
#  push:
#    branches:
#      - 
#    paths:
#      - '.config'
#  schedule:
#    - cron: 0 8 * * 5
  watch:
    types: [started]
    
env:
  ROM_URL: https://cloud189-liaoning-person.oos-lnsy.ctyunapi.cn/e32b2548-c80c-4297-acf3-f3309b272519?x-amz-UFID=11541210602329155&x-amz-CLIENTNETWORK=UNKNOWN&x-amz-SHID=169344678&x-amz-FSIZE=2051256752&Expires=1592301074&x-amz-UID=741439743&response-content-disposition=attachment%3Bfilename%3D%22MIUI12mi6_06_16.zip%22&AWSAccessKeyId=532482dfdc85e5d60592&x-amz-limitrate=10240&x-amz-CLOUDTYPEIN=PERSON&x-amz-CLIENTTYPEIN=WEB&Signature=c9Na8LXxqR9gqoJsXNAnp75xoAs%3D
  ROM_NAME: MIUI
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id

    steps:
       - name: Checkout
         uses: actions/checkout@master
         
       - name: Cleaning up
         run: |
           docker rmi `docker images -q`

       - name: Cloning ErfanGSIs tools
         run: |
            git clone --recurse-submodules https://github.com/erfanoabdi/ErfanGSIs.git
            sudo chmod -R 777 ErfanGSIs
             
       - name: Setting up Porting Environment
         run: |
           cd ErfanGSIs
           sudo apt update
           sudo bash setup.sh
       
#       - name: Download Stock Rom & Generate GSI
#         run: sudo ./ErfanGSIs/url2GSI.sh $ROM_URL $ROM_NAME --ab
       
       - name: Download Stock Rom & Generate Aonly GSI
         run: sudo ./ErfanGSIs/url2GSI.sh -a $ROM_URL $ROM_NAME
       
       - name: 7z Aonly GSI
         run: |
           7za a -t7z -r MI6-06-16_Aonly.7z ErfanGSIs/output/*
       
       - name: Upload GSI
         run: |
              curl -sL https://git.io/file-transfer | sh
              sudo ./transfer bit MI6-06-16_Aonly.7z
